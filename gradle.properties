# The IDE ships its own Kotlin standard library; bundling a second copy
# would make the plugin larger and can clash with the platform's version.
kotlin.stdlib.default.dependency=false
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
